scripted_gui = {
	APDC_super_event_ui = {
		window_name = "APDC_super_event_window"
		context_type = player_context

		visible = {
			has_variable = APDC_super_event
		}

		effects = {
			APDC_super_event_comment_button_click = {
				clear_variable = APDC_super_event
			}
		}

		properties = {
			APDC_super_event_picture = {
				image = "GFX_APDC_super_event_picture_[?APDC_super_event]"
			}
		}
	}
}
